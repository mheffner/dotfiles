# Include HAML files
--type-set
haml=.haml

# Include SASS files
--type-add
sass=.sass

# Include JSON files
--type-add
json=.json

# Include coffee files (XXX: as JS for now)
--type-add
js=.coffee

# Ignore bundler directory
--ignore-dir=.bundle
